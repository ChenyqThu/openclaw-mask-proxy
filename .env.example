# Proxy listen port
PORT=8081

# Upstream API service (e.g. sub2api, CRS, or any Anthropic-compatible endpoint)
UPSTREAM_HOST=127.0.0.1
UPSTREAM_PORT=8080

# Optional: override the API key sent to upstream
# Leave empty to passthrough the original key
UPSTREAM_API_KEY=

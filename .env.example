# Proxy listen port
PORT=8081

# Upstream API service (e.g. sub2api, CRS, or any Anthropic-compatible endpoint)
UPSTREAM_HOST=127.0.0.1
UPSTREAM_PORT=8080

# Default mask word when model ID has no @suffix (default: Code)
DEFAULT_MASK_WORD=Code
